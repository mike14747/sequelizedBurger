<div class="form-check">
    <input id="patty{{id}}" class="form-check-input" type="radio" name="patty" value="{{id}}">
    <label class="form-check-label" for="patty{{id}}">
        {{patty}}
    </label>
</div>